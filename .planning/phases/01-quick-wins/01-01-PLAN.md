---
phase: 01-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - backend/config.toml
  - backend/config.toml.example
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - NET-03

must_haves:
  truths:
    - "config.toml is untracked by git — git status shows it as untracked, not modified"
    - "*.env, *.key, *.pem, *.cert files are ignored by git"
    - "A fresh cargo run binds to 127.0.0.1 — ss -tlnp shows no 0.0.0.0 for server port"
    - "config.toml.example exists in git with empty token for onboarding"
  artifacts:
    - path: ".gitignore"
      provides: "Root gitignore protecting secrets from version control"
      contains: "backend/config.toml"
    - path: "backend/config.toml"
      provides: "Server config with secure default bind address"
      contains: 'host = "127.0.0.1"'
    - path: "backend/config.toml.example"
      provides: "Template config committed to git with empty token"
      contains: 'token = ""'
  key_links:
    - from: ".gitignore"
      to: "backend/config.toml"
      via: "gitignore pattern excludes config.toml from tracking"
      pattern: "backend/config.toml"
---

<objective>
Eliminate passive secret leaks through git and fix insecure default bind address.

Purpose: config.toml contains the auth token which grants full shell access. It is currently tracked by git, meaning every push exposes the token. The default bind address 0.0.0.0 exposes the server on all network interfaces. Both are zero-effort fixes with high security impact.

Output: Root .gitignore protecting secrets, config.toml untracked, bind address defaulting to 127.0.0.1, config.toml.example for onboarding.
</objective>

<execution_context>
@/home/gluz/.claude/get-shit-done/workflows/execute-plan.md
@/home/gluz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quick-wins/01-RESEARCH.md
@.planning/phases/01-quick-wins/01-VALIDATION.md

Key context from research:
- config.toml is currently tracked (committed in 106e2e8) -- `git rm --cached` is required before .gitignore takes effect
- `git rm --cached` does NOT delete the file from disk
- backend/config.toml line 2: `host = "0.0.0.0"` -- change to `127.0.0.1`
- main.rs reads config.server.host dynamically -- no code change needed for bind address
- No config.toml.example exists -- create one with `token = ""` for onboarding
- Token already in git history at commit 106e2e8 -- history rewrite is out of scope
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root .gitignore and untrack config.toml</name>
  <files>.gitignore</files>
  <action>
    1. Run `git rm --cached backend/config.toml` to untrack the file from git's index without deleting it from disk.
    2. Create root `.gitignore` at the repository root with these patterns:

       backend/config.toml
       *.env
       *.key
       *.pem
       *.cert

    3. Verify with `git status` that backend/config.toml now appears as "deleted" (from index) and is listed under untracked (or not listed at all due to .gitignore).
    4. Verify with `git check-ignore backend/config.toml` that the gitignore rule matches.

    IMPORTANT: Do NOT delete backend/config.toml from disk. `git rm --cached` is the correct command -- it only removes from git's tracking index.
  </action>
  <verify>
    <automated>git rm --cached backend/config.toml 2>/dev/null; git check-ignore backend/config.toml && echo "PASS: config.toml ignored" || echo "FAIL: config.toml not ignored"</automated>
  </verify>
  <done>config.toml is no longer tracked by git. Root .gitignore exists with patterns for config.toml, *.env, *.key, *.pem, *.cert. `git check-ignore` confirms all patterns match.</done>
</task>

<task type="auto">
  <name>Task 2: Change default bind address and create config example</name>
  <files>backend/config.toml, backend/config.toml.example</files>
  <action>
    1. Edit `backend/config.toml`: change `host = "0.0.0.0"` to `host = "127.0.0.1"` on line 2. No other changes.

    2. Create `backend/config.toml.example` with this exact content (committed to git as onboarding template):

       [server]
       host = "127.0.0.1"
       port = 48322

       [auth]
       token = ""

       [terminal]
       scrollback_lines = 10000
       default_shell = "/bin/bash"

    The example file has `token = ""` so the app auto-generates a token on first run. This file IS committed to git (not in .gitignore).

    3. Add a unit test `default_bind_address` in `backend/src/config.rs` inside the existing `#[cfg(test)] mod tests` block:

       #[test]
       fn default_bind_address() {
           let toml_str = std::fs::read_to_string("config.toml")
               .unwrap_or_else(|_| include_str!("../config.toml.example").to_string());
           let config: Config = toml::from_str(&toml_str).unwrap();
           assert_eq!(config.server.host, "127.0.0.1");
       }

    This test verifies the shipped default is 127.0.0.1 by parsing the example config.
  </action>
  <verify>
    <automated>cd /home/gluz/code/remote-control/backend && cargo test default_bind_address 2>&1</automated>
  </verify>
  <done>backend/config.toml has host = "127.0.0.1". backend/config.toml.example exists with token = "" and host = "127.0.0.1". Unit test default_bind_address passes.</done>
</task>

</tasks>

<verification>
1. `git check-ignore backend/config.toml` exits 0 (config.toml ignored)
2. `git check-ignore test.env test.key test.pem test.cert` exits 0 for all patterns
3. `git status backend/config.toml` does NOT show config.toml as tracked/modified
4. `grep 'host = "127.0.0.1"' backend/config.toml` matches
5. `test -f backend/config.toml.example` passes
6. `cargo test -p remote-control-backend default_bind_address` passes
</verification>

<success_criteria>
- config.toml is untracked by git (SEC-01)
- *.env, *.key, *.pem, *.cert are gitignored (SEC-02)
- Default bind address is 127.0.0.1 (NET-03)
- config.toml.example committed for onboarding
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-wins/01-01-SUMMARY.md`
</output>
